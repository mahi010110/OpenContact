Pastille d'indice de complétude (0–100) en IBM Plex Mono : texte sombre sur lavis, vert dès 70, ambre dès 40, rouge sinon. Bouton si `onClick` (toucher = explication). Indicateur d'entretien de la fiche, jamais présenté comme une garantie d'exactitude.

```jsx
<Score value={82} onClick={explain} />
<Score value={45} />
<Score value={12} />
```
