Liste déroulante système, même creux que Input, chevron pixel-art à droite.

```jsx
<Select options={['Récentes', 'A → Z', 'Plus proches']} aria-label="Trier" />
<Select options={[{value:'all', label:'Tous les domaines'}]} />
```
