Interrupteur rectangulaire : curseur carré biseauté qui saute en 2 pas (pas de glissement fluide), piste teal quand actif.

```jsx
<Switch label="Mode sombre" checked={dark} onChange={e => setDark(e.target.checked)} />
```
