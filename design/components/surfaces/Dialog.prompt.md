Modale : fenêtre à barre marine posée sur un voile tramé (le dither remplace le flou de fond).

```jsx
<Dialog open={open} title="Ajouter une piste" icon="plus" onClose={close}
        footer={<><Button variant="ghost">Annuler</Button><Button variant="primary">Enregistrer</Button></>}>
  …formulaire…
</Dialog>
```
